#include <stdlib.h>
#include <stdio.h>

int main (int argc, char **argv) {
   int i=0;
   int a = 2;
   int b = 3;

   printf("Hello world!\n");

   if ((a<b))
   {
     i=((a+b)*c);
     h=a-b;
     s=(b=a)*c;
     s23=fin0%e2;
     return1a = log(10)*sin(2,pi,p2, 21, 30) - pow(a, 2);
   }
   else 
   {
      i = 5 + 7 - 2*3;
   }
   
   if (a > b) {
	f;
   }
   
   return 0;
}
