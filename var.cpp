#include <iostream>
#include "var.h"

long Var::isVar(string s) {
	
	
	return s.length();
}

long Var::isWord(string s) {
	
	
	return s.length();
}

long Var::isFunc(string s) {
	
	
	return s.length();
}
